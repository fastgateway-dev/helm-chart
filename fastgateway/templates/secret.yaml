{{/*
No secrets are created by this chart.
Users must create secrets externally and reference them via:
- secrets.existingSecret (keys: jwt-secret, encryption-key, admin-password)
- database.existingSecret (keys: username, password, database)
*/}}
